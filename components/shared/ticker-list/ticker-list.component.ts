import { Component } from "@angular/core";
import { ROUTER_DIRECTIVES } from "@angular/router";

import { Stock } from "../../models/_models";

@Component({
  selector: "ticker-list",
  templateUrl: "./components/shared/ticker-list/ticker-list.html",
  providers: [
  ]
})

export class TickerListComponent {
  public 

  public constructor() {
  }
}

import "./ticker-list.html";
import "./ticker-list.scss";
